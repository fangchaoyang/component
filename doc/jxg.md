```
Jxg中绘制多个图形时，若绘制过多(十几个)，(不建议对单个图形进行删除、重绘)，性能会很差

    可先绘制全部图形，后对其进行显示、隐藏。但是若同时对十几个图形进行显示、隐藏操作，也会导致性能很卡，故建议创建多个board对象，
    将这些图形平均放入board中，并在vue中创建多个div对这些board分别进行显示、隐藏。
```
 
```
使用Jxg绘制的坐标轴或图形，鼠标移至其上会发生颜色闪动

    需添加属性 highlight = false , 可解决该问题
```